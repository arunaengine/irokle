Drop every integrity verdict and scan in progress, so the next integrity
question scans the stored records again. Admission cannot introduce a hole, but
damage from outside irokle can, and nothing else would ever look a second time.
