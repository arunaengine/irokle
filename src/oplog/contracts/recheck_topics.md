Drop the record of which topics were found whole, so the next integrity question
audits the stored records again. Admission cannot introduce a hole, but damage
from outside irokle can, and nothing else would ever ask a second time.
